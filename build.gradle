plugins {
  id "org.sonarqube" version "2.7"
}

sonarqube {
  properties {
    property "sonar.projectKey", "gradle:tuduapp"
    property "sonar.projectName", "Tudu App : Gradle"
    property "sonar.sources", "src/"
  }
}

